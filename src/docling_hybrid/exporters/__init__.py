"""Export functionality for multiple output formats.

STATUS: STUB - Extended scope (Phase 2)

This module will provide exporters for:
- Markdown (export_to_markdown)
- HTML (export_to_html)
- JSON (export_to_json)
- RAG chunks (export_to_rag_chunks)

Currently, basic Markdown export is handled directly in the orchestrator.
This module will be implemented when block-level processing is added.

Planned Usage:
    from docling_hybrid.exporters import MarkdownExporter, HtmlExporter
    
    exporter = MarkdownExporter(config)
    markdown = exporter.export(hybrid_document)
"""

# Placeholder for future implementation
__all__ = []
