"""Integration tests for docling-hybrid-ocr.

These tests require:
- OPENROUTER_API_KEY environment variable
- PDF files in pdfs/ directory (files starting with '2511')

Run with:
    pytest tests/integration -v
"""
